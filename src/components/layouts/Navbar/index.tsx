import styles from "./Navbar.module.css";

const Navbar = () => {
  return (
    <div className={styles.navbar}>
      <div>Navbar</div>
      <div>Logout</div>
    </div>
  );
};

export default Navbar;
